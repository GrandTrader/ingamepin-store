import CustomerAccountShell from "../CustomerAccountShell";
import { formatCustomerDate, requireCustomer } from "@/lib/customer-account-data";
export const dynamic = "force-dynamic";
export default async function CustomerNotificationsPage() { const { user, supabase, displayName } = await requireCustomer(); const result = await supabase.from("customer_notifications").select("id, notification_type, title, message, is_read, created_at").eq("user_id", user.id).order("created_at", { ascending: false }).limit(100); const notifications = result.data ?? []; return <CustomerAccountShell displayName={displayName}><p className="text-xs font-black uppercase tracking-[0.2em] text-cyan-600">Customer account</p><h1 className="mt-2 text-3xl font-black">Notifications</h1><p className="mt-2 text-slate-500">Order, payment, and account updates.</p><div className="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">{notifications.length === 0 ? <p className="p-7 text-slate-500">No notifications yet.</p> : notifications.map((notification) => <article key={notification.id} className={`border-b p-5 last:border-b-0 ${notification.is_read ? "" : "bg-cyan-50/60"}`}><div className="flex flex-wrap justify-between gap-2"><div><p className="text-xs font-black uppercase text-cyan-600">{notification.notification_type}</p><h2 className="mt-1 font-black">{notification.title}</h2></div><time className="text-xs text-slate-400">{formatCustomerDate(notification.created_at)}</time></div><p className="mt-2 text-sm leading-6 text-slate-600">{notification.message}</p></article>)}</div></CustomerAccountShell>; }
