import CustomerAccountShell from "../CustomerAccountShell";
import { requireCustomer } from "@/lib/customer-account-data";
import { updateCustomerPhone } from "./actions";
const countryCodes = [["India", "+91"], ["Russia", "+7"], ["United States / Canada", "+1"], ["United Kingdom", "+44"], ["Bangladesh", "+880"], ["Pakistan", "+92"], ["United Arab Emirates", "+971"], ["Saudi Arabia", "+966"], ["Turkey", "+90"], ["Ukraine", "+380"], ["Germany", "+49"], ["France", "+33"], ["Spain", "+34"], ["Italy", "+39"], ["Netherlands", "+31"], ["Poland", "+48"], ["China", "+86"], ["Japan", "+81"], ["South Korea", "+82"], ["Indonesia", "+62"], ["Malaysia", "+60"], ["Thailand", "+66"], ["Vietnam", "+84"], ["Brazil", "+55"], ["Mexico", "+52"]] as const;
export const dynamic = "force-dynamic";
export default async function CustomerProfilePage({ searchParams }: { searchParams: Promise<{ error?: string; success?: string }> }) { const messages = await searchParams; const { user, supabase, displayName } = await requireCustomer(); const profile = await supabase.from("profiles").select("name, phone").eq("id", user.id).maybeSingle(); const savedPhone = profile.data?.phone || String(user.user_metadata?.phone ?? ""); return <CustomerAccountShell displayName={displayName}><p className="text-xs font-black uppercase tracking-[0.2em] text-cyan-600">Customer account</p><h1 className="mt-2 text-3xl font-black">Profile</h1><p className="mt-2 text-slate-500">Your personal and account information.</p>{messages.error && <p className="mt-5 rounded-xl border border-red-200 bg-red-50 p-3 text-sm font-bold text-red-700">{messages.error}</p>}{messages.success && <p className="mt-5 rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-sm font-bold text-emerald-700">{messages.success}</p>}<section className="mt-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="grid gap-5 sm:grid-cols-2"><div><p className="text-xs font-bold uppercase text-slate-400">Full name</p><p className="mt-1 font-bold">{profile.data?.name || displayName}</p></div><div><p className="text-xs font-bold uppercase text-slate-400">Email</p><p className="mt-1 break-all font-bold">{user.email}</p></div><div><p className="text-xs font-bold uppercase text-slate-400">Phone</p><p className="mt-1 font-bold">{savedPhone || "Not added"}</p></div><div><p className="text-xs font-bold uppercase text-slate-400">Email status</p><p className="mt-1 font-bold text-emerald-600">Verified</p></div></div><details className="mt-7 max-w-xl border-t border-slate-200 pt-6" open={!savedPhone}><summary className="cursor-pointer font-black text-cyan-700">{savedPhone ? "Change phone number" : "Add phone number"}</summary><form action={updateCustomerPhone} className="mt-5 grid gap-4 sm:grid-cols-[220px_1fr]"><label className="text-sm font-bold">Country code<select name="country_code" defaultValue="+91" className="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3">{countryCodes.map(([country, code]) => <option key={`${country}-${code}`} value={code}>{country} ({code})</option>)}</select></label><label className="text-sm font-bold">Phone number<input name="phone" type="tel" inputMode="tel" required placeholder="98765 43210" className="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3 outline-none focus:border-cyan-500" /></label><button type="submit" className="rounded-xl bg-cyan-500 px-6 py-3 font-black text-slate-950 sm:col-span-2 sm:w-fit">Save phone number</button></form></details></section></CustomerAccountShell>; }
