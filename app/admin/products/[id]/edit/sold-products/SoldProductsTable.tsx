"use client";
import Link from "next/link";
import { useMemo, useState } from "react";
export type SoldProductRow = { saleId: string; orderId: string; orderNumber: string; soldAt: string | null; optionName: string; customerEmail: string; paid: string };
export default function SoldProductsTable({ productId, rows }: { productId: string; rows: SoldProductRow[] }) {
  const [search, setSearch] = useState(""); const filtered = useMemo(() => { const query = search.trim().toLowerCase(); return query ? rows.filter((row) => row.orderNumber.toLowerCase().includes(query) || row.customerEmail.toLowerCase().includes(query)) : rows; }, [rows, search]);
  return <><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search by email or Order ID" className="mt-5 w-full max-w-lg rounded-xl border border-slate-200 px-4 py-3" /><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead className="bg-slate-50 text-slate-500"><tr><th className="px-4 py-3">Order</th><th className="px-4 py-3">Date</th><th className="px-4 py-3">Product / denomination</th><th className="px-4 py-3">Buyer</th><th className="px-4 py-3">Paid</th><th className="px-4 py-3">Status</th></tr></thead><tbody>{filtered.map((row) => <tr key={row.saleId} className="border-t border-slate-200"><td className="px-4 py-3"><Link href={`/admin/orders/${row.orderId}/receipt`} className="font-bold text-blue-600 hover:underline">{row.orderNumber}</Link></td><td className="px-4 py-3">{row.soldAt ? new Date(row.soldAt).toLocaleString() : "—"}</td><td className="px-4 py-3"><Link href={`/admin/products/${productId}/edit/sold-products/${row.saleId}`} className="font-bold text-blue-600 hover:underline">{row.optionName}</Link></td><td className="px-4 py-3">{row.customerEmail}</td><td className="px-4 py-3 font-bold">{row.paid}</td><td className="px-4 py-3"><span className="rounded-full bg-emerald-50 px-2 py-1 text-xs font-bold text-emerald-700">Sold</span></td></tr>)}</tbody></table>{filtered.length === 0 && <p className="p-8 text-center text-slate-500">No matching sold products.</p>}</div></>;
}
